PASS Stand then one Kanban immediately enters Box
PASS one Kanban checks many Boxes without requesting more Kanbans
PASS one Box can finish only with explicit BOX complete
PASS wrong Kanban does not set reference or advance
PASS wrong Box does not count and blocks finish until corrected
PASS invalid parser result leaves reference and count unchanged
PASS skip Stand still compares every Box against Kanban
PASS clear last Box retains Stand and Kanban
PASS clear only Box requires another Box before finishing
PASS clear Kanban before Boxes goes back to Kanban
PASS clear Stand before Kanban goes back to Stand
PASS clear last item never dismisses a rejected scan
PASS completed batch is immutable and has nothing removable
PASS new batch clears previous references counts and error
PASS customer callback is used for all comparisons
PASS Box list cannot be externally cleared
Passed 16 production-flow scenarios

XML: 4 files parsed; all 18 UI IDs resolved.
TagParser.kt and EmployeeParser.kt preserved byte-for-byte from v0.15.
Clear controls statically verified to remain visible; UI/device behavior not tested.
SQLite upgraded in place from schema 5 to 6; no table/data deletion. Legacy completed sessions remain NOT_READY and are not uploaded automatically.
Central sync statically verified: fixed Apps Script /exec endpoint, INTERNET and ACCESS_NETWORK_STATE permissions, WorkManager CONNECTED constraint, PENDING/SENDING/SYNCED states, retry queue, session_id duplicate protection contract.
Dashboard statically verified to show central sync state before Outlook. If still pending, user must choose wait or continue to Outlook.
Google Apps Script doGet was confirmed by the user to return API is ready. No production POST fixture was sent to avoid creating a false production row.
Added CentralPayloadJsonTest for JSON escaping, session/event fields, parse-result mapping, and explicit API ok=true response.
NOT RUN HERE: Kotlin/Android compilation, full JUnit including CentralPayloadJsonTest, APK build, Android device UI test, end-to-end production POST.

v0.18.1 compile hotfix:
- exportAndShare now shows the AlertDialog then returns Unit explicitly; it no longer attempts to return AlertDialog from a Unit function.
- CentralSession is now returned directly from Cursor.use; it is no longer assigned to an outer val inside a lambda.
