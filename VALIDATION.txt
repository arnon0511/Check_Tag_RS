PASS Stand then one Kanban immediately enters Box
PASS one Kanban checks many Boxes without requesting more Kanbans
PASS one Box can finish only with explicit BOX complete
PASS wrong Kanban does not set reference or advance
PASS wrong Box does not count and blocks finish until corrected
PASS invalid parser result leaves reference and count unchanged
PASS skip Stand still compares every Box against Kanban
PASS clear last Box retains Stand and Kanban
PASS clear only Box requires another Box before finishing
PASS clear Kanban before Boxes goes back to Kanban
PASS clear Stand before Kanban goes back to Stand
PASS clear last item never dismisses a rejected scan
PASS completed batch is immutable and has nothing removable
PASS new batch clears previous references counts and error
PASS customer callback is used for all comparisons
PASS Box list cannot be externally cleared
Passed 16 production-flow scenarios

XML: 4 files parsed; all 18 UI IDs resolved.
TagParser.kt and EmployeeParser.kt preserved byte-for-byte from v0.15.
Clear controls statically verified to remain visible; UI/device behavior not tested.
SQLite upgraded in place from schema 5 to 6; no table/data deletion. Legacy completed sessions remain NOT_READY and are not uploaded automatically.
Central sync statically verified: fixed Apps Script /exec endpoint, INTERNET and ACCESS_NETWORK_STATE permissions, WorkManager CONNECTED constraint, PENDING/SENDING/SYNCED states, retry queue, session_id duplicate protection contract.
Dashboard statically verified to show central sync state before Outlook. If still pending, user must choose wait or continue to Outlook.
Google Apps Script doGet was confirmed by the user to return API is ready. No production POST fixture was sent to avoid creating a false production row.
Added CentralPayloadJsonTest for JSON escaping, session/event fields, parse-result mapping, and explicit API ok=true response.
NOT RUN HERE: Kotlin/Android compilation, full JUnit including CentralPayloadJsonTest, APK build, Android device UI test, end-to-end production POST.

v0.18.1 compile hotfix:
- exportAndShare now shows the AlertDialog then returns Unit explicitly; it no longer attempts to return AlertDialog from a Unit function.
- CentralSession is now returned directly from Cursor.use; it is no longer assigned to an outer val inside a lambda.

v0.18.2 DNTH parser update:
- Bottom Part No. before 01 anchors DISC parsing.
- Variable DNTH suffix length 4..10 supported for Kanban, Stand and Box.
- Joined upper Part No. + seven-digit quantity supported.
- Added tests for TG053661-7020S2, TG028993-6160 and conflicting upper/lower values.

v0.22.0 comparison and parser update:
- Added EXACT / WARNING / MISMATCH comparison levels for every customer Part No.
- Same family before the first hyphen but different remaining characters is WARNING, never silent OK.
- Different family before the first hyphen is MISMATCH and cannot advance.
- WARNING requires a reason once per Part No. pair and remains WARNING on Dashboard/CSV.
- DNTH DISC uses TG/TGY and the lower Part No. before 01; C07 and lane are not required anchors.
- Added case-insensitive DNTH test without C07/lane and JTCS B01-attached sample test.
- Local environment has no Gradle/Kotlin compiler; GitHub Actions must confirm JUnit and APK build.

v0.22.2 optional Pick List update:
- Employee scan opens a non-cancelable choice: compare Pick List or skip it.
- Compare routes to PICK_LIST; skip routes directly to KANBAN.
- The flow bar explicitly displays skipped Pick List state.
- Delivery Order remains QR-only; manual quantity entry was not restored.

v0.22.3 one-document flow correction:
- Pick List and Delivery Order use the same generated QR and are scanned once.
- Both compare and skip modes must scan the document QR to load QTY and BOX.
- Document QR is scanned before KANBAN.
- Compare mode checks document Part No. against KANBAN; skip mode does not.
- KANBAN remains the reference for Stand and Box in both modes.

v0.22.4 quantity-first optional comparison correction:
- Both modes require manual Current QTY and NO. OF BOX greater than zero first.
- Compare mode: quantity/boxes -> Pick List/Delivery Order QR -> KANBAN.
- Skip mode: quantity/boxes -> KANBAN; document QR is skipped.
- Document QR no longer overwrites the manually confirmed quantity or box count.

v0.22.5 optional Stand correction:
- After KANBAN is accepted, operator must choose whether to inspect Stand.
- Check Stand routes to STAND and compares it with KANBAN.
- Skip Stand routes directly to BOX; Box is compared with KANBAN only.
- The progress bar omits Stand when Stand inspection is skipped.

v0.22.6 position-prefix and warning UI correction:
- Treat one-letter/two-digit leading location codes such as B01 and C01 as metadata, not Part No.
- Attached forms such as C01JGC10-003770-40 are parsed as JGC10-003770-40, case-insensitively.
- Part No. WARNING confirmation dialog uses a light orange background.
- GitHub Actions uses setup-android v4 with an empty packages input so it does not request the removed legacy `tools` package; required SDK 35 packages are installed explicitly afterward.
